import styles from './styles.module.css'

const TestComponent = () => {
    return (
        <div className={styles.test_container}>
            <h1 className={styles.title}>TestingComponent</h1>
        </div>
    );
};

export default TestComponent;